
  # golden brunch site

  This is a code bundle for golden brunch site. The original project is available at https://www.figma.com/design/kWDclx0ZzStMFYH7AsrXDH/golden-brunch-site.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  